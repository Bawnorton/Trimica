{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "name": "${mod_name}",
  "version": "${mod_version}",
  "description": "${mod_description}",
  "environment": "*",
  "accessWidener": "${minecraft_version}.accesswidener",
  "entrypoints": {
    "main": [
      "com.bawnorton.trimica.platform.fabric.FabricEntrypoint"
    ],
    "fabric-datagen": [
      "com.bawnorton.trimica.platform.fabric.data.FabricDataGeneratorEntrypoint"
    ]
  }
}